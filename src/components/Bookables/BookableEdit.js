import {
    Typography
} from "@material-ui/core";

const BookableEdit = () => {
    return (
        <Typography variant="h6" component="h6">Edit Bookable</Typography>
    );
}

export default BookableEdit;